package com.yodlee.support.microservice.controller;

/*
public class PostApiTest {
	
	@Autowired
	AuthorizationService authService;
	
	@Autowired
	MomentService momentService;
	
	@Autowired
	MongoRepositry mongo;
	
	@Test
	public void testLoginAndGetMomentsApi() {
		
		Login login=authService.login("","");
		
		assertionService.assert(login);
		
		Moments monets=momentService.getAllMoments(login.token());
		
		assertionService.assert(moments);
		
		//mongo.findByToken(token);
		
	}
	
}
*/